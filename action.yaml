name: Probot Example
description: "Example Action built with Probot"
branding:
  icon: "gift"
  color: orange
runs:
  using: "composite"
  steps:
    - run: ${GITHUB_ACTION_PATH}/entrypoint.sh
      shell: bash
